{{/*
SPDX-FileCopyrightText: Copyright (C) 2022-2026 Cosmo Tech
SPDX-License-Identifier: MIT
*/}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cosmotech-gateway.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cosmotech-gateway.labels" . | nindent 4 }}
  annotations:
    email: "platform@cosmotech.com"
spec:
  strategy:
  {{- with .Values.deploymentStrategy }}
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "cosmotech-gateway.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "cosmotech-gateway.defaultNetworkPolicy" . | nindent 8 }}
        {{- include "cosmotech-gateway.selectorLabels" . | nindent 8 }}
    spec:
      {{- if .Values.imageCredentials.registry }}
      imagePullSecrets:
        - name: {{ include "cosmotech-gateway.fullname" . }}-registry
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      automountServiceAccountToken: false
      volumes:
        - name: helm-config
          configMap:
            name: {{ include "cosmotech-gateway.fullname" . }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            #
            # Some env vars here are properties that affect the way the Spring Application Context
            # is configured.
            # They need to be set here *before* the Application Context starts
            #
            - name: SPRING_PROFILES_INCLUDE
              # The properties file for the 'helm' profile is mounted as a Volume in the Container,
              # using a configured Kubernetes Secret containing all the 'config.*' properties
              value: "helm"
            - name: {{ include "cosmotech-gateway.identityClientSecretEnvVarName" . }}
              valueFrom:
                secretKeyRef:
                  name: {{ include "cosmotech-gateway.identitySecretName" . }}
                  key: clientSecret
          volumeMounts:
            - mountPath: /config
              name: helm-config
              readOnly: true
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
            - name: http-management
              containerPort: {{ .Values.service.managementPort }}
              protocol: TCP
          startupProbe:
            initialDelaySeconds: {{ .Values.probes.startup.initialDelaySeconds }}
            periodSeconds: 5
            failureThreshold: {{ .Values.probes.startup.failureThreshold }}
            tcpSocket:
              port: http
          livenessProbe:
            timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds }}
            periodSeconds: 5
            failureThreshold: {{ .Values.probes.liveness.failureThreshold }}
            httpGet:
              path: /actuator/health/liveness
              port: http-management
          readinessProbe:
            timeoutSeconds: {{ .Values.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold }}
            httpGet:
              path: /actuator/health/readiness
              port: http-management
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      affinity:
      {{- if .Values.affinity }}
      {{- with .Values.affinity }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- else }}
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: "app.kubernetes.io/name"
                      operator: In
                      values:
                        - {{ include "cosmotech-gateway.name" . }}
                    - key: "app.kubernetes.io/instance"
                      operator: In
                      values:
                        - {{ .Release.Name }}
                topologyKey: "kubernetes.io/hostname"
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
